entity.ebms_rejection_reason.collection:
  path: '/admin/config/ebms/ebms_rejection_reason'
  defaults:
    _entity_list: 'ebms_rejection_reason'
    _title: 'Rejection Reasons'
  requirements:
    _permission: 'administer site configuration'

entity.ebms_rejection_reason.add_form:
  path: '/admin/config/ebms/ebms_rejection_reason/add'
  defaults:
    _entity_form: 'ebms_rejection_reason.add'
    _title: 'Add Rejection Reason'
  requirements:
    _permission: 'administer site configuration'

entity.ebms_rejection_reason.edit_form:
  path: '/admin/config/ebms/ebms_rejection_reason/{ebms_rejection_reason}'
  defaults:
    _entity_form: 'ebms_rejection_reason.edit'
    _title: 'Edit Rejection Reason'
  requirements:
    _permission: 'administer site configuration'

entity.ebms_rejection_reason.delete_form:
  path: '/admin/config/ebms/ebms_rejection_reason/{ebms_rejection_reason}/delete'
  defaults:
    _entity_form: 'ebms_rejection_reason.delete'
    _title: 'Delete Rejection Reason'
  requirements:
    _permission: 'administer site configuration'
